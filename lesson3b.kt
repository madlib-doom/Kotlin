fun main() {

    // while loop 
    // structure
    // Initialize with an initial value
    // while keyword,condition to be evaluated,code to be executed if condition is true.increment/decrement
    // val ~Dictates constant value i.e cannot change
    // var ~Dictates a value that can chabge overtime/.. 
    var count=0
     while(count<=10){
        println("It is looping: $count")

        count++
     }


}